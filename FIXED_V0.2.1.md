# v0.2.1 verification trigger

- Fixed Yahoo symbol normalization so `^TASI.SR` is not changed to `^TASI.SR.SR`.
- Added a regression test for Saudi and TASI ticker formatting.
- Tightened CI/lint quality rules and timezone handling.
- Paper/Shadow only; zero-paid-services policy unchanged.
